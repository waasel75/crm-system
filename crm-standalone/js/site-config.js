'use strict';
/* Realtime Database REST URL for project "crm-system-app".
   Verify in Firebase console > Realtime Database > copy the URL shown at the top. */
const SITE = {
  firebaseUrl: 'https://crm-system-app-default-rtdb.firebaseio.com',
};
